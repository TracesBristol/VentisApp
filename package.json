{
  "name": "ventisApp",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "connect": "MONGO_URL='mongodb://<USERNAME>:<PASSWORD>@<SERVER ADDRESS>:3000/meteor' meteor run",
    "winConnect": "set MONGO_URL=mongodb://<USERNAME>:<PASSWORD>@<SERVER ADDRESS>:3000/meteor; meteor run"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.4"
  }
}
